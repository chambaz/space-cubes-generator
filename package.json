{
  "name": "space-cube-generator",
  "version": "0.0.0",
  "description": "Generate randomized WebGL Space Cubes, generate screenshots and meta data for NFT collection.",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "generate-cubes": "rm -rf dist && node generate-cubes.js",
    "generate-meta": "rm -rf dist/meta && node generate-meta.js",
    "organise-cubes": "node organise-cubes.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^13.0.0"
  }
}
